# Copia a .env junto al docker-compose.yml de este directorio.
# Esta misma contrasena la usan todas las instancias de cliente en su
# DATABASE_URL, asi que guardala bien.
POSTGRES_ADMIN_PASSWORD=cambia-esta-contrasena-larga-y-aleatoria
