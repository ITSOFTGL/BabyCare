# apps/mobile — reservada para v2

Esta carpeta está **reservada para la versión 2** de KidCare: una app móvil con
**Expo + React Native** para padres y profesoras.

No hay nada implementado todavía y **no debe implementarse en v1**. Cuando llegue
el momento, la app consumirá exactamente la misma API de `apps/api` y reutilizará
los tipos de `packages/types`.

Pendiente para v2 (junto con chat por sala, comunicados masivos, Web Push,
factura PDF y 2FA):

- Proyecto Expo con router y sesión JWT compartida con la web.
- Notificaciones push nativas.
- Vista de agenda diaria optimizada para móvil.
